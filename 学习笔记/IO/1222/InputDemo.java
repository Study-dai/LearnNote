package package1222;

import java.io.*;
import java.io.InputStream;
import java.util.Arrays;
import java.util.Scanner;
/*
public class InputDemo {
    private static InputStream is;
    private InputStream is;

    public static void main(String[] args) throws IOException {
        //用一个字节流得到字符流
        Reader reader = new InputStreamReader(is, "utf8");
        char [] buffer=new char[1024];
        int len=reader.read(buffer);
        System.out.println(Arrays.toString(buffer));
        return new String(buffer,0,len);

        Reader reader1=new InputStreamReader(is,"UTF8");
        BufferedReader bufferedReader=new BufferedReader(reader1);
        return bufferedReader.readLine();


        Scanner sc=new Scanner(is,"utf8");
    }

    public InputDemo() throws IOException {
    }
}
*/